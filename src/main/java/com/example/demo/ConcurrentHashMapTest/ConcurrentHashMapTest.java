package com.example.demo.ConcurrentHashMapTest;

import java.util.concurrent.ConcurrentHashMap;

/**
 * @create 2021-12-31 21:53
 */
public class ConcurrentHashMapTest {
    public static void main(String[] args) {
        //<init>,get,put,remove,putIfAbsent,containsKey,clear,values,size,keySet,entrySet,isEmpty,computeIfAbsent,
        //putAll,replace,toString,newKeySet,keys,contains,compute,getOrDefault,forEach,containsValue,hashCode,elements,
        //computeIfPresent,equals,merge,hash,mappingCount,replaceAll,entryAt,setEntryAt,forEachValue,ensureSegment,
        //entryForHash,segmentForHash,segmentAt,forEachKey,clone,search,searchValues,reduce
        new ConcurrentHashMap();
    }
}

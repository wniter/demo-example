package com.example.demo.MapTest;

/**
 * @create 2021-12-31 21:53
 */
public class TreeMapTest {
    public static void main(String[] args) {
        //<init>,put,get,entrySet,values,size,keySet,
        // remove,containsKey,isEmpty,clear,firstKey,putAll,
        // lastKey,firstEntry,tailMap,lastEntry,floorEntry,headMap,
        // subMap,clone,comparator,ceilingEntry,equals,hashCode,
        // higherEntry,pollFirstEntry,toString,descendingMap,containsValue,
        // descendingKeySet,floorKey,ceilingKey,higherKey,computeIfAbsent,
        // lowerEntry,navigableKeySet,forEach,pollLastEntry,lowerKey,rotateLeft,
        // rotateRight,getOrDefault,replaceInParent,find,internalPollFirstEntry,internalPollLastEntry,
        // putIfAbsent,rebalance,removeInternalByKey,
    }
}

package com.example.demo.MapTest;

/**
 * @create 2021-12-29 22:55
 */
public class HashMapTest {
    public static void main(String[] args) {
        //put,get,containsKey,keySet,remove,entrySet,values,size,clear,isEmpty,putAll,clone,toString,
        //containsValue,equals,hashCode,computeIfAbsent,getOrDefault,forEach,putIfAbsent,compute,merge,replace,

    }
}
